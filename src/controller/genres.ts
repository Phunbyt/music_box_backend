import express, { Request, Response, NextFunction } from 'express';
require('dotenv').config();

let url = process.env.GENRE_LINK as string
export async function genres(req: express.Request, res: express.Response) {

    
}

export async function genre(req: express.Request, res: express.Response) {
    
}